# Prompts

Prompt templates live here, but they must not contain real PHI.

Use synthetic examples only.

Any prompt that can include PHI at runtime must be routed through the BAA gate before leaving the local machine.
